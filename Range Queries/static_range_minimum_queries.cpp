#include<bits/stdc++.h>
#define int long long int
#define endl "\n"
using namespace std;

signed main(){
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	cout.tie(NULL);
	int n, q;
	cin >> n >> q;
	int A[n];
    for(int i = 0; i < n; i++){
        cin >> A[i];
    }
    // Minimum prefix array => P[i] denotes minimum 
    int P[n];
}
